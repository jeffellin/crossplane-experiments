apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: kubernetes-sb-function
spec:
  compositeTypeRef:
    apiVersion: ellin.net/v1alpha1
    kind: XMyHelmishDataStore
  mode: Pipeline
  pipeline:
  - step: patch-and-transform
    functionRef:
      name: function-patch-and-transform
    input:
      apiVersion: pt.fn.crossplane.io/v1beta1
      kind: Resources
      resources:
      - name: storage-bucket
        base:
          apiVersion: s3.aws.upbound.io/v1beta1
          kind: Bucket
          metadata:
            name: garbage-in-jellin-1
          spec:
            forProvider:
              region: "us-east-2"
            providerConfigRef:
              name: aws-provider-266463974589
  - step: run-the-template
    functionRef:
      name: kubernetes-sb-function
